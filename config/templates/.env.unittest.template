# ===========================================
# 测试环境配置模板
# 复制此文件为 .env.unittest 并填写实际值
# ===========================================

# ===========================================
# 环境标识
# ===========================================
NODE_ENV=unittest

# ===========================================
# 数据库配置 (测试环境MySQL)
# ===========================================
# 可以使用独立的测试数据库，或者与开发环境相同的数据库但不同的数据库名
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your-test-password
DB_DATABASE=contract_ledger_test
DB_POOL_SIZE=5

# ===========================================
# Redis配置 (测试环境Redis)
# ===========================================
# 可以使用相同的Redis服务器但不同的数据库编号
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# ===========================================
# JWT配置
# ===========================================
JWT_SECRET=your-test-jwt-secret-key-at-least-32-characters-long
JWT_EXPIRES_IN=24h

# ===========================================
# 应用配置
# ===========================================
BACKEND_PORT=8080
LOG_LEVEL=info
CORS_ORIGINS=*
