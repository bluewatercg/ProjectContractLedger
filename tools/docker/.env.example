# Docker 环境变量配置文件示例
# 复制此文件为 .env 并根据实际情况修改配置

# === 镜像配置 ===
GITHUB_REPOSITORY=your-username/your-repo
IMAGE_TAG=latest

# === 端口配置 ===
FRONTEND_HTTP_PORT=80
BACKEND_PORT=8080
MYSQL_PORT=3306
REDIS_PORT=6379

# === 数据库配置 ===
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=contract_user
DB_PASSWORD=your-secure-password-change-this
DB_DATABASE=contract_ledger
MYSQL_ROOT_PASSWORD=your-root-password-change-this

# === 应用配置 ===
NODE_ENV=production
JWT_SECRET=your-jwt-secret-key-change-this-to-a-secure-random-string
LOG_LEVEL=info
CONSOLE_LOG_LEVEL=info
LOG_DIR=/app/logs

# === TypeORM 配置 ===
TYPEORM_SYNCHRONIZE=false
TYPEORM_MIGRATIONS_RUN=true

# === Redis 配置 ===
REDIS_HOST=redis
REDIS_PASSWORD=
REDIS_DB=0

# === CORS 配置 ===
CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:3000

# === 文件上传配置 ===
UPLOAD_MAX_SIZE=10mb
UPLOAD_DIR=/app/uploads
