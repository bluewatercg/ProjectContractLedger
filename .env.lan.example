# ===========================================
# 局域网IP部署配置示例
# ===========================================
# 适用于在局域网内使用固定IP部署的场景
# 例如：192.168.1.115

# ===========================================
# 环境标识
# ===========================================
NODE_ENV=production

# ===========================================
# 应用端口配置
# ===========================================
# 前端HTTP端口（nginx提供静态文件和API代理）
APP_HTTP_PORT=8000
# 后端API端口
APP_API_PORT=8080

# ===========================================
# 数据库配置 (外部MySQL服务器)
# ===========================================
# MySQL服务器地址（局域网IP）
DB_HOST=192.168.1.254
DB_PORT=3306
DB_USERNAME=procontractledger
DB_PASSWORD=your_database_password
DB_DATABASE=procontractledger
DB_POOL_SIZE=20

# ===========================================
# 外部Redis服务器配置
# ===========================================
# Redis服务器地址（局域网IP）
REDIS_HOST=192.168.1.160
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
REDIS_DB=13

# ===========================================
# JWT配置
# ===========================================
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# ===========================================
# 应用配置
# ===========================================
LOG_LEVEL=info
# CORS配置：允许所有局域网访问
CORS_ORIGINS=*
TZ=Asia/Shanghai

# ===========================================
# 前端配置（运行时注入）
# ===========================================
# API基础URL配置
# 选项1：使用nginx代理（推荐，前后端在同一容器）
FRONTEND_API_BASE_URL=/api

# 选项2：直接访问后端（前后端分离部署时使用）
# FRONTEND_API_BASE_URL=http://192.168.1.115:8080/api/v1

# 应用标题
APP_TITLE=客户合同管理系统
APP_VERSION=1.0.0

# ===========================================
# 网络配置
# ===========================================
NETWORK_MODE=bridge

# ===========================================
# 部署说明
# ===========================================
# 1. 复制此文件为 .env
# 2. 修改数据库和Redis的IP地址
# 3. 设置JWT密钥和数据库密码
# 4. 运行: docker-compose -f docker-compose.production.yml up -d
# 5. 访问: http://192.168.1.115:8000 (替换为实际服务器IP)
