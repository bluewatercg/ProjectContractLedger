---
phase: 01-business-category-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/entity/business-category.entity.ts
  - apps/backend/src/service/business-category.service.ts
  - apps/backend/src/controller/business-category.controller.ts
  - apps/backend/src/interface.ts
  - database/migrations/create_business_categories_table.sql
autonomous: true

must_haves:
  truths:
    - "Backend API can create business categories with parent-child relationships"
    - "Backend API can retrieve full category tree for a kit"
    - "Backend API can update category name and status"
    - "Backend API can delete categories with validation"
    - "Backend API can move categories (change parent and sort order)"
  artifacts:
    - path: "apps/backend/src/entity/business-category.entity.ts"
      provides: "BusinessCategory entity with self-referencing relations"
      min_lines: 80
      contains: "@ManyToOne(() => BusinessCategory"
    - path: "apps/backend/src/service/business-category.service.ts"
      provides: "Tree operations (getTree, create, update, delete, moveNode)"
      min_lines: 200
      exports: ["getTree", "create", "update", "delete", "moveNode", "toggleStatus"]
    - path: "apps/backend/src/controller/business-category.controller.ts"
      provides: "REST endpoints for category management"
      min_lines: 100
      exports: ["getTree", "create", "update", "delete", "moveNode", "toggleStatus"]
    - path: "database/migrations/create_business_categories_table.sql"
      provides: "Database schema for business_categories table"
      contains: "parent_id INT NULL"
  key_links:
    - from: "apps/backend/src/controller/business-category.controller.ts"
      to: "apps/backend/src/service/business-category.service.ts"
      via: "@Inject() businessCategoryService"
      pattern: "businessCategoryService\\.(getTree|create|update|delete|moveNode)"
    - from: "apps/backend/src/service/business-category.service.ts"
      to: "apps/backend/src/entity/business-category.entity.ts"
      via: "@InjectEntityModel(BusinessCategory)"
      pattern: "categoryRepository\\.(find|save|delete|createQueryBuilder)"
---

<objective>
Build the backend infrastructure for hierarchical business category management with multi-tenant isolation.

Purpose: Establish the data model, business logic, and API endpoints for managing N-level business category trees. This enables the frontend to create, read, update, delete, and reorganize categories while maintaining Kit-based multi-tenancy.

Output: Complete backend API for business category CRUD operations and tree manipulation, ready for frontend integration.
</objective>

<execution_context>
@C:\Users\miller\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\miller\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-business-category-infrastructure/01-RESEARCH.md
@.planning/CODING_PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Create BusinessCategory entity with self-referencing tree structure</name>
  <files>
    apps/backend/src/entity/business-category.entity.ts
  </files>
  <action>
Create TypeORM entity following the Adjacency List pattern from research. The entity must:

1. Use self-referencing @ManyToOne/@OneToMany relations for parent-child hierarchy
2. Include all required fields: id, kit_id, parent_id (nullable), name, sort_order, status (enum: active/disabled), created_by, created_at, updated_at
3. Add @JoinColumn decorators for foreign keys (parent_id, kit_id, created_by)
4. Follow existing entity patterns from customer.entity.ts (use same decorators, naming conventions)
5. Add Chinese comments for all columns matching existing codebase style
6. Status enum: 'active' (default) and 'disabled'
7. Table name: 'business_categories'

Reference the exact pattern from 01-RESEARCH.md Pattern 1 (lines 70-136) but ensure it matches the coding patterns in CODING_PATTERNS.md (Entity 规范, lines 332-366).
  </action>
  <verify>
Run: cd apps/backend && yarn build
Check: No TypeScript compilation errors
Check: Entity file exports BusinessCategory class with all decorators
  </verify>
  <done>
BusinessCategory entity exists with self-referencing parent/children relations, kit_id foreign key, sort_order column, and status enum. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Implement BusinessCategoryService with tree operations</name>
  <files>
    apps/backend/src/service/business-category.service.ts
  </files>
  <action>
Create service following existing service patterns from CODING_PATTERNS.md (Service 规范, lines 232-330). Implement these methods:

1. **getTree(kitId: number)**: Load all categories for kit, build tree structure in memory using the algorithm from 01-RESEARCH.md Pattern 2 (lines 138-176). Return root nodes with nested children arrays.

2. **create(dto: CreateBusinessCategoryDto, kitId: number, createdBy: number)**: Create new category. If parent_id provided, calculate sort_order as max(siblings.sort_order) + 1. Set kit_id and created_by.

3. **update(id: number, dto: UpdateBusinessCategoryDto, kitId: number)**: Update name and/or status. Validate category exists and belongs to kit.

4. **delete(id: number, kitId: number)**: Delete with validation:
   - Check for children (return error if has children)
   - Check for associated contracts (future: query contract count, for now return mock count 0)
   - Return { success: boolean, message?: string, contractCount?: number }

5. **moveNode(nodeId, targetId, dropType: 'prev'|'inner'|'next', kitId)**: Implement drag-drop logic from 01-RESEARCH.md Pattern 4 (lines 238-306). Update parent_id and sort_order, shift siblings as needed.

6. **toggleStatus(id: number, kitId: number)**: Toggle between 'active' and 'disabled'.

All methods must:
- Use @Provide() decorator
- Inject repository via @InjectEntityModel(BusinessCategory)
- Filter by kit_id in all queries
- Use QueryBuilder for complex queries
- Throw Error with Chinese messages for validation failures
  </action>
  <verify>
Run: cd apps/backend && yarn build
Check: Service exports all 6 methods
Check: All methods accept kitId parameter
Check: getTree returns nested tree structure (test with mock data if needed)
  </verify>
  <done>
BusinessCategoryService implements all tree operations (getTree, create, update, delete, moveNode, toggleStatus) with kit isolation. All methods compile and follow existing service patterns.
  </done>
</task>

<task type="auto">
  <name>Create REST API controller and DTOs</name>
  <files>
    apps/backend/src/controller/business-category.controller.ts
    apps/backend/src/interface.ts
  </files>
  <action>
1. **Add DTOs to interface.ts**:
   - CreateBusinessCategoryDto: { name: string, parent_id?: number }
   - UpdateBusinessCategoryDto: { name?: string, status?: string }
   - MoveNodeDto: { nodeId: number, targetId: number, dropType: 'prev'|'inner'|'next' }
   - Use @ApiProperty decorators for Swagger documentation

2. **Create controller** following CODING_PATTERNS.md Controller 规范 (lines 151-230):
   - @Controller('/api/v1/business-categories')
   - @ApiTags(['业务类型管理'])
   - Inject BusinessCategoryService and Context
   - Extract kitId from ctx.state.kitId (set by KitMiddleware)
   - Extract userId from ctx.state.user.id (set by AuthMiddleware)

3. **Implement endpoints**:
   - GET / - getTree() - Returns full tree for current kit
   - GET /?status=active - getTree with status filter (for dropdowns)
   - POST / - create(@Body() dto) - Create new category
   - PUT /:id - update(@Param('id') id, @Body() dto) - Update category
   - DELETE /:id - delete(@Param('id') id) - Delete with validation
   - POST /move - moveNode(@Body() dto) - Handle drag-drop
   - PATCH /:id/toggle-status - toggleStatus(@Param('id') id) - Toggle active/disabled

All endpoints must:
- Use @Validate() decorator for DTOs
- Wrap responses in ApiResponse format: { success: boolean, data?, message? }
- Use try-catch to handle service errors
- Add Swagger decorators (@ApiOperation, @ApiBody, @ApiOkResponse)
  </action>
  <verify>
Run: cd apps/backend && yarn build
Check: Controller compiles without errors
Check: All 7 endpoints defined with proper decorators
Check: DTOs added to interface.ts with validation decorators
  </verify>
  <done>
Controller exposes 7 REST endpoints for business category management. All endpoints follow existing patterns, use kit isolation, and return standardized ApiResponse format. DTOs defined with Swagger documentation.
  </done>
</task>

</tasks>

<verification>
1. Backend compiles: `cd apps/backend && yarn build` succeeds
2. Entity has self-referencing relations: Check BusinessCategory.parent and BusinessCategory.children decorators
3. Service implements all 6 methods: getTree, create, update, delete, moveNode, toggleStatus
4. Controller exposes 7 endpoints: GET /, POST /, PUT /:id, DELETE /:id, POST /move, PATCH /:id/toggle-status
5. All code follows kit isolation pattern: Every query includes kit_id filter
6. DTOs defined with validation: CreateBusinessCategoryDto, UpdateBusinessCategoryDto, MoveNodeDto in interface.ts
</verification>

<success_criteria>
- BusinessCategory entity exists with parent_id self-reference and kit_id foreign key
- BusinessCategoryService provides complete tree manipulation API
- REST controller exposes all CRUD and tree operations
- All code compiles without TypeScript errors
- Kit isolation enforced in all service methods
- API follows existing codebase patterns (controller/service/entity structure)
</success_criteria>

<output>
After completion, create `.planning/phases/01-business-category-infrastructure/01-01-SUMMARY.md`
</output>
