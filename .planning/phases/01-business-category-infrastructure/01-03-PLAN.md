---
phase: 01-business-category-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - database/migrations/create_business_categories_table.sql
  - apps/backend/src/configuration.ts
autonomous: false

must_haves:
  truths:
    - "Database table business_categories exists with correct schema"
    - "Backend server starts without errors"
    - "Frontend can load empty tree (no errors)"
    - "User can create a root category and see it in tree"
    - "User can create child categories under root"
    - "User can drag-drop to reorder categories"
    - "User can delete a leaf category"
    - "User can toggle category status"
  artifacts:
    - path: "database/migrations/create_business_categories_table.sql"
      provides: "SQL migration script for business_categories table"
      min_lines: 15
      contains: "CREATE TABLE business_categories"
  key_links:
    - from: "apps/backend/src/entity/business-category.entity.ts"
      to: "database/migrations/create_business_categories_table.sql"
      via: "TypeORM entity maps to table"
      pattern: "business_categories"
---

<objective>
Create database migration and perform end-to-end integration testing of the business category feature.

Purpose: Ensure the database schema is properly created and the full stack (frontend -> backend -> database) works together. Verify all user-facing requirements are met through manual testing.

Output: Working database table and verified end-to-end functionality for business category management.
</objective>

<execution_context>
@C:\Users\miller\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\miller\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-business-category-infrastructure/01-RESEARCH.md
@.planning/phases/01-business-category-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-business-category-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create database migration script</name>
  <files>
    database/migrations/create_business_categories_table.sql
  </files>
  <action>
Create SQL migration script based on the schema from 01-RESEARCH.md (lines 369-388). The script must:

1. Create table with all columns matching the BusinessCategory entity:
   - id INT AUTO_INCREMENT PRIMARY KEY
   - kit_id INT NOT NULL (FK to kits)
   - parent_id INT NULL (self-referencing FK)
   - name VARCHAR(100) NOT NULL
   - sort_order INT DEFAULT 0
   - status ENUM('active', 'disabled') DEFAULT 'active'
   - created_by INT NOT NULL (FK to users)
   - created_at DATETIME DEFAULT CURRENT_TIMESTAMP
   - updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP

2. Add foreign key constraints:
   - kit_id -> kits(id)
   - parent_id -> business_categories(id) ON DELETE RESTRICT
   - created_by -> users(id)

3. Add indexes for performance:
   - idx_kit_parent (kit_id, parent_id) - for tree queries
   - idx_sort_order (parent_id, sort_order) - for ordering

4. Use ENGINE=InnoDB DEFAULT CHARSET=utf8mb4

Include a comment header with migration name and date.
  </action>
  <verify>
Check: SQL file exists with CREATE TABLE statement
Check: All columns match entity definition
Check: Foreign keys defined correctly
Check: Indexes created for kit_id and parent_id
  </verify>
  <done>
Migration script created with complete schema for business_categories table including foreign keys and indexes.
  </done>
</task>

<task type="auto">
  <name>Register entity in TypeORM configuration</name>
  <files>
    apps/backend/src/configuration.ts
  </files>
  <action>
Check if TypeORM is configured to auto-discover entities via glob pattern (e.g., `**/entity/*.entity{.ts,.js}`).

If auto-discovery is configured: No changes needed - BusinessCategory entity will be picked up automatically.

If entities are explicitly listed: Add BusinessCategory to the entities array:
```typescript
import { BusinessCategory } from './entity/business-category.entity';
// Add to entities array
```

Also verify the controller is being scanned. Midway.js typically auto-scans controllers in the controller/ directory, but confirm the pattern includes the new controller.
  </action>
  <verify>
Run: cd apps/backend && yarn build
Run: cd apps/backend && yarn dev (start server briefly to check for entity registration errors)
Check: No "Entity not found" or "No metadata for BusinessCategory" errors
  </verify>
  <done>
BusinessCategory entity registered with TypeORM. Server starts without entity-related errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete business category management feature:
- Backend: Entity, Service, Controller with 7 API endpoints
- Frontend: Tree management page with drag-drop, CRUD dialogs
- Database: Migration script for business_categories table
  </what-built>
  <how-to-verify>
**Setup:**
1. Apply database migration: Run the SQL script in `database/migrations/create_business_categories_table.sql` against your MySQL database
2. Start backend: `cd apps/backend && yarn dev`
3. Start frontend: `cd apps/frontend && yarn dev`
4. Open browser to http://localhost:8000
5. Login with valid credentials

**Test Scenarios:**

1. **Navigate to page:**
   - Click "业务类型" in sidebar menu
   - Expected: Tree page loads, shows empty state or existing categories

2. **Create root category:**
   - Click "添加根分类" button
   - Enter name: "服务"
   - Click "确定"
   - Expected: Category appears in tree

3. **Create child category:**
   - Click "添加子分类" on "服务" node
   - Enter name: "运维服务"
   - Click "确定"
   - Expected: Child appears under "服务"

4. **Create another child:**
   - Add "角膜类运维" under "运维服务"
   - Expected: 3-level hierarchy visible

5. **Edit category:**
   - Click "编辑" on any category
   - Change name
   - Click "确定"
   - Expected: Name updates in tree

6. **Drag-drop reorder:**
   - Create another root category "产品"
   - Drag "产品" above "服务"
   - Expected: Order changes, persists after page refresh

7. **Drag-drop reorganize:**
   - Drag a child category to different parent
   - Expected: Category moves to new parent

8. **Toggle status:**
   - Click "禁用" on a category
   - Expected: Shows "已禁用" tag, drag disabled for this node

9. **Delete category:**
   - Try to delete a category with children
   - Expected: Error message about children
   - Delete a leaf category (no children)
   - Expected: Category removed

10. **Refresh persistence:**
    - Refresh the page
    - Expected: All changes persisted, tree structure intact
  </how-to-verify>
  <resume-signal>
Type "approved" if all 10 test scenarios pass.
If issues found, describe what failed and any error messages.
  </resume-signal>
</task>

</tasks>

<verification>
1. Database migration script exists and is syntactically correct
2. Backend starts without entity registration errors
3. All 10 manual test scenarios pass
4. Data persists across page refreshes
5. Multi-level hierarchy works (at least 3 levels tested)
6. Drag-drop works for both reordering and reorganizing
</verification>

<success_criteria>
- Database table created successfully
- Backend API responds correctly to all endpoints
- Frontend tree displays and manipulates categories
- All CRUD operations work end-to-end
- Drag-drop reordering and reorganization functional
- Status toggle works correctly
- Delete validation prevents deleting categories with children
- Changes persist in database
</success_criteria>

<output>
After completion, create `.planning/phases/01-business-category-infrastructure/01-03-SUMMARY.md`
</output>
